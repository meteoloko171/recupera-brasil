{
  "installCommand": "pnpm install",
  "buildCommand": "PORT=3000 BASE_PATH=/ pnpm --filter @workspace/solicitacao-saque run build && pnpm --filter @workspace/api-server run build:vercel && (if [ -n \"$DATABASE_URL\" ]; then pnpm --filter @workspace/db run push; else echo 'DATABASE_URL is not set yet -- skipping database migration. Connect a Postgres database in Vercel (Storage tab) and redeploy.'; fi)",
  "outputDirectory": "artifacts/solicitacao-saque/dist/public",
  "functions": {
    "api/index.js": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
